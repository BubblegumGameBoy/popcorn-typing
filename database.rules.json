{
  "rules": {
    ".read": false,
    ".write": false,
    "popcorn-typing": {
      "players": {
        ".read": true,
        ".indexOn": ["score"],
        "$playerId": {
          ".write": "!data.exists() || newData.child('score').val() >= data.child('score').val()",
          ".validate": "newData.hasChildren(['name', 'score']) && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 12 && newData.child('score').isNumber() && newData.child('score').val() >= 0"
        }
      }
    }
  }
}
